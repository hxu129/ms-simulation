# @package _global_
# Small model configuration for fast experimentation and testing

experiment_name: ms_predictor_small

model:
  hidden_dim: 256
  num_encoder_layers: 3
  num_decoder_layers: 3
  num_heads: 4
  dim_feedforward: 1024

data:
  batch_size: 32  # Moderate batch size
  num_workers: 0  # Use 0 to avoid worker process OOM
  max_files: 1   # CRITICAL: Limit files to avoid OOM (10 files ≈ 2GB when cached)

training:
  num_epochs: 100
  save_interval: 10


